# Exercise: React Component Props

Implement a reusable `Card` component and a `UserProfile` that uses it.

## Objectives

- Accept and destructure props with default values
- Conditionally render based on props
- Use children prop for flexible composition
